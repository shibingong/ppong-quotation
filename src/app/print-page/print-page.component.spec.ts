import { PrintPageComponent } from './print-page.component';

describe('PrintPageComponent', () => {
  let component: PrintPageComponent;

  beforeEach(() => {
    component = new PrintPageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
